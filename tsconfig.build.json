{
  "extends": "./tsconfig.json", // 继承根目录的配置
  "compilerOptions": { "emitDeclarationOnly": true }, // 只生成声明文件(d.ts)
  "include": ["src/**/*"], // 包含的文件夹
  "exclude": ["**/__tests__/**", "**/demo/**", "node_modules", "lib", "esm"] // 排除示例、测试以及打包好的文件夹
}
